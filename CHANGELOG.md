# 更新记录

## v2.6.0

- 环境判定覆盖有线中继：认证服务器短暂探测不到时，有线接路由器（无 SSID）环境也按校园网处理并尝试重登，不再误判「非校园网」后休眠（新增 is_campus_locked 判定 + 5 项测试）。
- 合盖保活与守护解耦：App 启动即恢复合盖/休眠保持运行偏好（不依赖守护是否启动）；停止守护不再误停用户已开启的合盖保活。
- 失败提示更具体：认证在线但出口不通 / 自动登录失败时，提示可能被其他设备占用名额、可在自助系统下线处理。

## v2.5.0

- 新增「防踢保活」：周期性刷新登录会话，让本机/路由器会话保持最新，名额满时第 3 台设备登录被挤掉的是别人而不是本设备（偏好设置可开关，默认开启）。
- 修复测速「VPN 对比失败」：VPN 对比测速两条路径独立容错，任一路径失败不再整体报错，成功路径照常显示结果。
- 实测确认中继场景会话归属（路由器 00:10:F3 占名额、登录按来源 IP 刷新续期），防踢与自动重登机制均验证可用。

## v2.4.0

- 新增「合盖/休眠时保持运行」：macOS 用 caffeinate 电源断言阻止系统/空闲睡眠，守护线程在合盖或系统空闲时继续联网保活，掉线后仍能自动重登（可在偏好设置中开关）。
- 修复中继场景「掉线后不动作」：绑定明确 SSID（如连中继路由器 WiFi / 直连 LIDA）时，即使认证服务器暂时探测不到，也按校园网环境处理并尝试重登，不再误判「非校园网」后静止休眠。
- 修复中继场景「重登失败」：档案匹配更智能，中继时优先选用已填写账号的校园网档案，避免落到空账号的默认档案导致登录失败。

## v2.3.3

- 自动登录成功后立即复检认证、VPN 和校园网直连路径。
- 自动同步刷新顶部网络状态、环境、匹配档案和检测时间。
- 修复慢速线路持续传输时因 25 秒上限被误报为测速失败的问题。
- 33 项自动化测试通过。

## v2.3.2

- 通知总开关关闭时，四类子通知立即取消勾选并禁用。
- 明确绿色对号表示已选择，点击“保存设置”后正式生效。

## v2.3.1

- macOS 复选框统一改为绿色底白色对号，未选中时显示空框。

## v2.3.0

- macOS 账号密码改存系统钥匙串，配置文件和安全导出不保存明文密码。
- 隧道共享新增启动自检、手机引导页、PAC 与二维码，保留新设备授权控制。
- 新增默认关闭的网络稳定性历史和面向普通用户的最近 7 天汇总。
- 新增系统通知总开关，以及掉线、恢复、恢复失败和新设备请求四类开关。

## v2.2.x

- 内置立达校园网专属档案。
- 新增多来源认证服务器探测，可识别 HTTP、HTML 与 JavaScript 跳转。
- VPN 状态与校园网直连状态分开判断，避免 VPN 场景误报假在线。
- 新增自动 VPN 对比测速、延迟、抖动、请求成功率和网络质量评分。
- 新增路由器只读体检、管理入口识别和安全刷机条件检查。
- 重做主界面固定布局，只有运行日志区域可独立滚动。
